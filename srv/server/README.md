# XLearn Main Server

## Makefile Documentation

- `make`: Default target that builds and runs the application
- `make build`: Compiles the project and creates a binary in the `bin` directory
- `make run`: Runs the application directly without creating a binary
- `make clean`: Removes the generated binary file
- `make tidy`: Updates Go module dependencies
- `make test`: Runs all tests in the project
